/*
 * This class is for the player sprite
 */
public class Player extends Sprite {
	
	/*
	 * Constructor, takes in x, y, width, and height
	 */
	public Player(int x, int y, int w, int h) {
		super(x, y, w, h);
	}
}
